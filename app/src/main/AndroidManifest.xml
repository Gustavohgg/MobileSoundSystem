<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        android:screenOrientation="portrait">

        <!-- LoginActivity como Launcher -->
        <activity
            android:name=".controlador.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity -->
        <activity
            android:name=".controlador.MainActivity"
            android:exported="true">
        </activity>


        <activity
            android:name=".controlador.FuncionariosActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".controlador.CadGerenteActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".controlador.CadVendedorActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".controlador.EstoqueActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".controlador.VendaActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".controlador.CadProdutoActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".controlador.CadClienteActivity"
            android:exported="true">
        </activity>

    </application>

</manifest>
